<nav class="flex mx-auto px-3 pb-2 gap-x-6 lg:gap-x-9 border-b border-aoc-gray-darker">

  <% if @user.confirmed? %>

    <%= render Header::NavTabComponent.new(link: { name: "calendar", path: calendar_path }) %>
    <%= render Header::NavTabComponent.new(link: { name: "scores", path: scores_path }) %>
    <%= render Header::NavTabComponent.new(link: { name: "the wall", path: messages_path }) %>
    <%= render Header::NavTabComponent.new(link: { name: "<i class=\"fas fa-gear\"></i>".html_safe, path: settings_path }) %>

  <% else %>

    <%= render Header::NavTabComponent.new(link: { name: "faq", path: faq_path }) %>
    <%= render Header::NavTabComponent.new(link: { name: "setup", path: setup_path }) %>

  <% end %>

  <%= link_to destroy_user_session_path, class: "hover:text-wagon-red" do %>
    <i class="fas fa-power-off"></i>
  <% end %>

</nav>
